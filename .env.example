# Public — safe to expose to the browser (Vite inlines VITE_* at build time)
SUPABASE_PROJECT_ID=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_URL=""
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Server-only secrets — set these in your host's environment (or a gitignored
# .env.local), never commit real values.
SUPABASE_SERVICE_ROLE_KEY=""
OPENROUTER_API_KEY=""
